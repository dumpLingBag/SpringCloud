# 不需要认证即可访问的路径
ignored:
  # 不验证 url
  urls:
    - /favicon.ico
    - /login/captcha
    - /system/listForMenu
    - /login/verify
    - /user/retrieve
  # 不验证 token
  pathsToSkip:
    - /login/captcha
    - /login/verify
    - /user/retrieve
token:
  processingPath: /**
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 47.98.232.27:18848
  application:
    name: service-authority